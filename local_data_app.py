from __future__ import annotations

import streamlit as st

st.set_page_config(
    page_title="本地数据管理 (已合并到主应用)",
    page_icon="🗄️",
    layout="wide",
    initial_sidebar_state="expanded",
)

st.title("🗄️ 本地数据管理入口已合并到主应用")
st.markdown(
    """
本地数据管理界面已经集成到主应用 `app.py` 中，现在只需要运行一个 Streamlit 服务。

**请按以下方式使用：**

1. 启动后端服务：
   `uvicorn tdx_backend:app --host 0.0.0.0 --port 9000`
2. 启动前端主应用：
   `streamlit run app.py --server.port 8501`
3. 在浏览器中打开主应用后，在侧边栏点击「🗄️ 本地数据管理」即可进入本地数据管理页面。

本文件仅用于兼容与提示，避免误启动旧的独立界面。
    """
)

